{
    "image":"mcr.microsoft.com/devcontainers/universal:2",
    "features": {
        "ghcr.io/devcontainers/features/node:1": {}
    },
    "postCreateCommand": "bash -i -c 'sudo apt update && sudo apt install libxshmfence1 && nvm install --lts && npm install -g npm'"
}